# TODO: OpenAPI重複回避の仮名 https://github.com/stlatica/stlatica/issues/248
UserID:
  description: |
    userを識別するための一意のID \
    actor_idのfrontend向けエイリアスであり、actor_idと同一の値を持つ
  type: string
  example: 'user_id'
